# ── Core (always required) ─────────────────────────────────────────────────
matplotlib>=3.8.0
sympy>=1.12
networkx>=3.2
Pillow>=10.2.0
numpy>=1.26.0
pandas>=2.2.0
scipy>=1.12.0
python-dotenv>=1.0.0
tqdm>=4.66.0
requests>=2.31.0

# ── GPT-4o / OpenAI API mode (optional) ───────────────────────────────────
openai>=1.12.0

# ── Gemini API mode (optional) ────────────────────────────────────────────
google-generativeai>=0.4.0

# ── HuggingFace LLaVA backend (optional, for HPC / GPU) ──────────────────
# Install with: pip install transformers accelerate
# For 4-bit / 8-bit quantisation on small GPUs also install:
# pip install bitsandbytes
transformers>=4.37.0
accelerate>=0.27.0
